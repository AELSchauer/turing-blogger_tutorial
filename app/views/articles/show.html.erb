<h1><%= @article.title %></h1>
<p><%= @article.body %></p>

<h3>Comments (<%= @article.comments.count %>)</h3>
<%= render partial: "comments/form" %>
<%= render partial: "articles/comment", collection: @article.comments.reverse %>

<%= link_to "<< Back to Articles List", articles_path, class: "top-button" %>
<%= link_to "Edit Article", edit_article_path(@article) %>
<%= link_to "Delete Article", article_path(@article), data: {confirm: "Do you really want to delete this article?"}, method: :delete %>